SELECT
SUM(BBB.VENDAS) AS VENDAS,
BBB.PERIODO

FROM
(SELECT
SUM(AAA.VENDAS) AS VENDAS,
AAA.PERIODO

FROM
(SELECT
CAB.VLRNOTA AS VENDAS,
LTRIM(TO_CHAR(CAB.DTNEG, 'mm/yyyy'), '0') AS PERIODO

FROM
TGFCAB CAB

WHERE
CAB.CODEMP = '2' AND
CAB.CODTIPOPER IN ('3200','3205','3250','3262')) AAA

GROUP BY
AAA.PERIODO) BBB

GROUP BY
BBB.PERIODO