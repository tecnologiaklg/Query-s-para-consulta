SELECT
EST.CODLOCAL,
LOC.DESCRLOCAL,
EST.CODPROD,
EST.CONTROLE,
EST.ESTOQUE,
PRO.DESCRPROD

FROM
TGFEST EST,
TGFPRO PRO,
TGFLOC LOC

WHERE
EST.CODPROD = PRO.CODPROD
AND EST.CODLOCAL = LOC.CODLOCAL
AND EST.CODEMP = '3'
